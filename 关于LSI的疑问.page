LSI使用 [SVD]() 来计算主题，因此有如下的等式：

$$A = T \Lambda D^T$$

其中，A 为词项－文档矩阵，行为词项，列为文档；T为词项－主题矩阵，D为主题－文档矩阵。它们满足如下关系：

$$T^T T = I_r \quad D^T D = I_r$$

$$\Lambda ＝ diag( \lambda_1, ..., \lambda_r )$$

我的问题是：

- 为什么 SVD 作为一种 [PCA]() 手段，它能捕捉到主要方向？
- 有什么方法能够确定降维到的合适维度？
